# Source masters — too large for static deploy, lives in Vercel Blob instead.
videos/

# Local encoder outputs — uploaded to Vercel Blob, not bundled.
encoded/

# Reference / mood-board PNGs — not used by the live site EXCEPT the
# wordmark, which the Trollolol page references.
images/
!images/trololol_transparent.png

# Project docs (specs, plans) — not for the deployed site.
docs/

# Local dev artifacts
.DS_Store
.env.local
.env*.local
node_modules/

# Test harness page is fine to ship; do NOT exclude tests/.
